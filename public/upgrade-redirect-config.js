/**
 * URL de upgrade (plano free / itens premium).
 * Este arquivo é servido como estático: altere aqui e recarregue a página —
 * não exige novo `npm run build` quando você usa `serve -s build` ou CDN.
 * Deixe string vazia ('') para usar só REACT_APP_UPGRADE_URL do build (após rebuild).
 */
window.__ONLYFLOW_UPGRADE_URL__ = 'https://onlyflow.com.br/#precos-cards';
